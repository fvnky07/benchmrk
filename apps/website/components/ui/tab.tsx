// Generated by OpenCode AI - Tab Component - 2026-02-07
// Source: https://github.com/aymanch-03/shadcn-pricing-page
import { domAnimation, LazyMotion, m } from 'motion/react';

import { cn } from '@/lib/utils';

import { Badge } from './badge';

interface TabProps {
  text: string;
  selected: boolean;
  setSelected: (text: string) => void;
  discount?: boolean;
}

export const Tab = ({
  text,
  selected,
  setSelected,
  discount = false,
}: TabProps) => {
  return (
    <LazyMotion features={domAnimation}>
      <button
        onClick={() => setSelected(text)}
        className={cn(
          'relative w-fit px-4 py-2 font-semibold text-black text-sm capitalize transition-colors',
          discount && 'flex items-center justify-center gap-2.5'
        )}
      >
        <span className="relative z-10">{text}</span>
        {selected && (
          <m.span
            layoutId="tab"
            transition={{ type: 'spring', duration: 0.4 }}
            className="absolute inset-0 z-0 rounded-full bg-white shadow-sm"
          ></m.span>
        )}
        {discount && (
          <Badge
            className={cn(
              'relative z-10 whitespace-nowrap bg-gray-100 text-black text-xs shadow-none hover:bg-gray-100',
              selected ? 'bg-[#00ff90]' : 'bg-gray-300'
            )}
          >
            Save 35%
          </Badge>
        )}
      </button>
    </LazyMotion>
  );
};
