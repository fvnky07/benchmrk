// Generated by OpenCode AI - Pricing Page with Frequency Toggle - 2026-02-07
// Source: https://github.com/aymanch-03/shadcn-pricing-page
'use client';

import { PricingCard } from '@/components/sections/pricing/TierCard';
import { PricingHeader } from '@/components/sections/pricing/PricingHeader';
import { PAYMENT_FREQUENCIES, TIERS } from '@/components/config';
import { useState } from 'react';

export default function PricingPage() {
  const [selectedPaymentFreq, setSelectedPaymentFreq] = useState(
    PAYMENT_FREQUENCIES[0]
  );

  return (
    <main className="flex min-h-screen items-center justify-center bg-black px-4 py-16">
      <section className="flex w-full max-w-4xl flex-col items-center gap-10 rounded-4xl border border-2 px-6 py-8 text-2xl 2xl:max-w-6xl">
        {/* Section Header */}
        <PricingHeader
          title="Plans and Pricing"
          subtitle="Receive unlimited credits when you pay yearly, and save on your plan."
          frequencies={PAYMENT_FREQUENCIES}
          selectedFrequency={selectedPaymentFreq}
          onFrequencyChange={setSelectedPaymentFreq}
        />

        {/* Pricing Cards */}
        <div className="grid w-full gap-6 sm:grid-cols-2">
          {TIERS.map((tier, i) => (
            <PricingCard
              key={i}
              tier={tier}
              paymentFrequency={selectedPaymentFreq}
            />
          ))}
        </div>
      </section>
    </main>
  );
}
