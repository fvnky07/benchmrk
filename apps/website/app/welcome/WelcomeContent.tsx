'use client';
// Generated by OpenCode AI - 2026-02-05
// NOTE: Client component for welcome page with animations and user data
import { useSession } from '@/lib/auth-client';
import {
  Sparkles,
  Trophy,
  Dumbbell,
  TrendingUp,
  Clock,
  HeadphonesIcon,
  Rocket,
  Twitter,
  CheckCircle2,
} from 'lucide-react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
import GridPattern from '@/components/ui/grid-pattern';

export default function WelcomeContent() {
  // NOTE: Get session from Better Auth client
  const { data: session } = useSession();

  // NOTE: Premium benefits list
  const benefits = [
    {
      icon: Dumbbell,
      title: 'AI Workout Coach',
      description:
        'Personalized recommendations that adapt to your progress and goals',
    },
    {
      icon: TrendingUp,
      title: 'Advanced Analytics',
      description:
        'Deep insights into your performance with beautiful visualizations',
    },
    {
      icon: Clock,
      title: 'Unlimited History',
      description: 'Access your complete workout history, forever',
    },
    {
      icon: HeadphonesIcon,
      title: 'Priority Support',
      description: 'Get help when you need it with dedicated premium support',
    },
    {
      icon: Rocket,
      title: 'All Future Features',
      description: 'Every new premium feature we release, automatically yours',
    },
  ];

  return (
    <main className="bg-black-1 relative flex min-h-screen flex-col items-center justify-center overflow-hidden px-4 py-12">
      <GridPattern
        width={30}
        height={30}
        strokeColor="rgba(255,255,255, 0.1)"
      />

      <div className="relative z-10 mx-auto max-w-3xl text-center">
        {/* Celebration header */}
        <div className="mb-8 inline-flex items-center gap-2 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-4 py-2">
          <CheckCircle2 className="h-5 w-5 text-emerald-400" />
          <span className="text-sm font-medium text-emerald-400">
            Email confirmed successfully
          </span>
        </div>

        {/* Main heading with celebration */}
        <div className="mb-6 flex justify-center">
          <div className="relative">
            <Trophy className="text-cyan-1 h-20 w-20" />
            <Sparkles className="absolute -top-2 -right-2 h-8 w-8 animate-pulse text-yellow-400" />
          </div>
        </div>

        <h1 className="mb-4 font-[nippo] text-4xl font-bold tracking-tight text-white sm:text-5xl md:text-6xl">
          Welcome to{' '}
          <span className="bg-gradient-to-r from-cyan-400 to-emerald-400 bg-clip-text text-transparent">
            benchmrk
          </span>
          !
        </h1>

        <p className="mb-2 text-xl text-gray-300 sm:text-2xl">
          You&apos;re officially a{' '}
          <span className="font-semibold text-emerald-400">
            Lifetime Premium
          </span>{' '}
          member
        </p>

        {session?.user && (
          <p className="mb-8 text-gray-500">
            Registered as{' '}
            <span className="text-gray-400">{session.user.email}</span>
          </p>
        )}

        {/* Benefits grid */}
        <div className="mb-10 rounded-3xl border border-gray-800 bg-gray-900/50 p-6">
          <h2 className="text-cyan-1 mb-6 text-lg font-semibold">
            What you get - forever
          </h2>

          <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {benefits.map((benefit) => (
              <div
                key={benefit.title}
                className="rounded-2xl border border-gray-800 bg-gray-800/50 p-4 text-left transition-all hover:border-cyan-500/30 hover:bg-gray-800"
              >
                <benefit.icon className="text-cyan-1 mb-3 h-6 w-6" />
                <h3 className="mb-1 font-medium text-white">{benefit.title}</h3>
                <p className="text-sm text-gray-400">{benefit.description}</p>
              </div>
            ))}
          </div>
        </div>

        {/* Launch announcement */}
        <div className="mb-8 rounded-2xl border border-yellow-500/30 bg-yellow-500/10 p-6">
          <Rocket className="mx-auto mb-3 h-8 w-8 text-yellow-400" />
          <h3 className="mb-2 text-lg font-semibold text-yellow-400">
            App Launching Soon
          </h3>
          <p className="text-gray-400">
            We&apos;re putting the finishing touches on benchmrk. You&apos;ll be
            the first to know when it&apos;s ready!
          </p>
        </div>

        {/* Social CTA */}
        <div className="flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
          <Button
            asChild
            className="bg-cyan-1 hover:bg-cyan-1/90 rounded-full px-6 py-3 font-semibold text-black"
          >
            <Link href="https://twitter.com/fvnky_07" target="_blank">
              <Twitter className="mr-2 h-4 w-4" />
              Follow for updates
            </Link>
          </Button>

          <Button
            asChild
            variant="outline"
            className="rounded-full border-gray-700 px-6 py-3 text-gray-300 hover:bg-gray-800"
          >
            <Link href="/">Back to home</Link>
          </Button>
        </div>
      </div>

      {/* Confetti-like decorative elements */}
      <div className="pointer-events-none absolute inset-0 overflow-hidden">
        <div className="bg-cyan-1/20 absolute top-1/4 left-1/4 h-2 w-2 animate-ping rounded-full" />
        <div
          className="absolute top-1/3 right-1/3 h-2 w-2 animate-ping rounded-full bg-emerald-500/20"
          style={{ animationDelay: '0.5s' }}
        />
        <div
          className="absolute bottom-1/3 left-1/3 h-2 w-2 animate-ping rounded-full bg-yellow-500/20"
          style={{ animationDelay: '1s' }}
        />
        <div
          className="bg-cyan-1/20 absolute right-1/4 bottom-1/4 h-2 w-2 animate-ping rounded-full"
          style={{ animationDelay: '1.5s' }}
        />
      </div>
    </main>
  );
}
