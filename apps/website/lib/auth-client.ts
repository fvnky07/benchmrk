// Generated by OpenCode AI - 2026-02-05
// NOTE: Better Auth client for frontend authentication
import { convexClient } from '@convex-dev/better-auth/client/plugins';
import { magicLinkClient } from 'better-auth/client/plugins';
import { createAuthClient } from 'better-auth/react';

export const authClient = createAuthClient({
  plugins: [
    // NOTE: Convex integration plugin
    convexClient(),
    // NOTE: Magic link client for passwordless auth
    magicLinkClient(),
  ],
});

// NOTE: Export commonly used hooks and methods
export const { signIn, signOut, useSession } = authClient;
