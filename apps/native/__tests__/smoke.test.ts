describe('Smoke Test', () => {
  it('should pass a basic assertion', () => {
    expect(1 + 1).toBe(2);
  });

  it('should verify jest-expo is configured', () => {
    expect(true).toBe(true);
  });
});
