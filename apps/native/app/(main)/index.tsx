// Generated by OpenCode AI - Auth Flow Setup - 2026-02-12
import { View, StyleSheet, ScrollView } from 'react-native';

import { SafeAreaView } from 'react-native-safe-area-context';

import { Text } from '@/components/ui/text';

/**
 * Home/Dashboard screen
 *
 * NOTE: This screen is only accessible when authenticated
 * TODO: Implement actual home screen content:
 * - User stats/metrics
 * - Recent activity
 * - Quick actions
 * - Data from Convex backend
 */
export default function HomeScreen() {
  return (
    <SafeAreaView style={styles.container} edges={['top']}>
      <ScrollView
        style={styles.scrollView}
        contentContainerStyle={styles.content}
      >
        <View style={styles.header}>
          <Text variant="h1" className="font-bold text-foreground">
            Home
          </Text>
          <Text variant="large" className="mt-2 text-muted-foreground">
            Welcome to your dashboard
          </Text>
        </View>

        <View style={styles.card} className="mt-6 rounded-2xl bg-card p-6">
          <Text variant="h3" className="mb-2 text-card-foreground">
            Getting Started
          </Text>
          <Text variant="p" className="text-muted-foreground">
            This is a placeholder home screen. Connect your Convex backend and
            Better Auth to add real functionality.
          </Text>
        </View>

        <View style={styles.card} className="mt-4 rounded-2xl bg-card p-6">
          <Text variant="h3" className="mb-2 text-card-foreground">
            Next Steps
          </Text>
          <Text variant="p" className="mb-3 text-muted-foreground">
            • Set up Better Auth integration
          </Text>
          <Text variant="p" className="mb-3 text-muted-foreground">
            • Connect to Convex backend
          </Text>
          <Text variant="p" className="mb-3 text-muted-foreground">
            • Add logout functionality
          </Text>
          <Text variant="p" className="text-muted-foreground">
            • Implement real data fetching
          </Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollView: {
    flex: 1,
  },
  content: {
    padding: 20,
  },
  header: {
    marginBottom: 8,
  },
  card: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 3,
  },
});
