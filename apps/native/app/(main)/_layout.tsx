// Generated by OpenCode AI - Auth Flow Setup - 2026-02-12
import { Platform } from 'react-native';

import { useColorScheme } from 'nativewind';

import { Tabs } from 'expo-router';

import { THEME } from '@/lib/theme';

/**
 * Main app layout with tabs navigation
 *
 * NOTE: This is only accessible when user is authenticated
 * Protected by Stack.Protected guard in app/_layout.tsx
 *
 * TODO: Add icons for tabs when ready
 * - Use @expo/vector-icons or custom icon component
 * - Add badges for notifications
 * - Customize tab bar appearance
 */
export default function MainLayout() {
  const { colorScheme } = useColorScheme();
  const colors = THEME[colorScheme ?? 'light'];

  return (
    <Tabs
      screenOptions={{
        headerShown: false,
        tabBarActiveTintColor: colors.primary,
        tabBarInactiveTintColor: colors.mutedForeground,
        tabBarStyle: {
          backgroundColor: colors.card,
          borderTopColor: colors.border,
          ...Platform.select({
            ios: {
              height: 88,
              paddingBottom: 34,
              paddingTop: 8,
            },
            android: {
              height: 64,
              paddingBottom: 8,
              paddingTop: 8,
            },
          }),
        },
        tabBarLabelStyle: {
          fontSize: 12,
          fontWeight: '600',
        },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: 'Home',
          // TODO: Add icon
          // tabBarIcon: ({ color, size }) => (
          //   <Icon name="home" size={size} color={color} />
          // ),
        }}
      />
      <Tabs.Screen
        name="explore"
        options={{
          title: 'Explore',
          // TODO: Add icon
        }}
      />
      <Tabs.Screen
        name="profile"
        options={{
          title: 'Profile',
          // TODO: Add icon
        }}
      />
    </Tabs>
  );
}
