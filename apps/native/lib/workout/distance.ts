export function distanceMetersToKilometers(distanceMeters: number): number {
  return distanceMeters / 1000;
}

export function distanceKilometersToMeters(distanceKilometers: number): number {
  return distanceKilometers * 1000;
}
