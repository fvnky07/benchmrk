// Generated by OpenCode AI - Auth Flow Setup - 2026-02-12
// NOTE: Placeholder auth hook - will be replaced with Convex + Better Auth integration
import { useState, useEffect } from 'react';

interface UseAuthReturn {
  isAuthenticated: boolean;
  isLoading: boolean;
  // TODO: Add auth methods when integrating Better Auth
  // login: (email: string, password: string) => Promise<void>;
  // logout: () => Promise<void>;
  // register: (email: string, password: string) => Promise<void>;
}

/**
 * Placeholder authentication hook
 *
 * TODO: Replace with real implementation:
 * - Integrate with Better Auth client
 * - Connect to Convex backend
 * - Add session management
 * - Handle token refresh
 * - Add error handling
 *
 * For now, this returns:
 * - isAuthenticated: false (forces users to auth screens)
 * - isLoading: simulates initial auth check
 */
export function useAuth(): UseAuthReturn {
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    // Simulate checking for existing session
    // TODO: Replace with actual session check from Better Auth
    const checkAuth = async () => {
      try {
        // Simulate network delay
        await new Promise((resolve) => setTimeout(resolve, 500));

        // TODO: Check for stored session/token
        // const session = await authClient.getSession();
        // setIsAuthenticated(!!session);

        setIsAuthenticated(false); // Default to not authenticated
      } catch (error) {
        console.error('Auth check failed:', error);
        setIsAuthenticated(false);
      } finally {
        setIsLoading(false);
      }
    };

    checkAuth();
  }, []);

  return {
    isAuthenticated,
    isLoading,
  };
}
