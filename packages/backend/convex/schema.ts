// Generated by OpenCode AI - 2026-02-05
// NOTE: Main app schema - waitlist table for tracking signups before confirmation
import { defineSchema, defineTable } from 'convex/server';
import { v } from 'convex/values';

export default defineSchema({
  // NOTE: Waitlist table for tracking users before they confirm via magic link
  // Once confirmed, they become a Better Auth user with isPremium flag
  waitlist: defineTable({
    email: v.string(),
    // NOTE: Track waitlist position for UI display
    position: v.optional(v.number()),
    // NOTE: Made optional for backwards compatibility with existing data
    createdAt: v.optional(v.number()),
  })
    .index('by_email', ['email'])
    .index('by_position', ['position']),
});
